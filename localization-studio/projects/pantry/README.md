# Pantry Lens: AI Recipes

Pantry is registered for `en-US` and `tr-TR` application/store copy. The six
text-free Story Mode assets and direct application JSON integration are the
remaining release-stage tasks. Pantry currently keeps its runtime copy in a
JavaScript catalog, so Localization Studio must not overwrite application copy
until that catalog is migrated to the shared JSON contract.
